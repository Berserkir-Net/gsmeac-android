<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.liamcottle.gsmeac">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/GSMEAC"
            android:name=".GSMEACApplication">

        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity.BriefEditorActivity" android:label="@string/activity_new_brief"/>
        <activity android:name=".activity.BriefViewerActivity" android:label="@string/activity_brief_viewer"/>

    </application>

</manifest>